package tests

import (
	"testing"
)

func TestStagen(t *testing.T) {
	t.Parallel()

	// ctx := context.Background()
	//
	// gitTool := git.New()
	//
	// stagenTool := stagen.New(gitTool)
	//
	// stagenTool.Init(ctx, stagenConfig, siteConfig)
}
